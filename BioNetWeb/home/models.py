from django.db import models

# Create your models here.
#class FilePackage(models):
    #user = 
   # bngl_file = models.FileField()
   # exp_file = models.Filefield()
   # config_file = models.FileField()
